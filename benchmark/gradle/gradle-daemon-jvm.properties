# Daemon JVM criteria for the benchmark build, which has its own wrapper like the demo. The same as
# the root build's gradle/gradle-daemon-jvm.properties, which carries the reasoning: Java 17 of any
# vendor, found locally, and a named failure without one.
toolchainVersion=17
