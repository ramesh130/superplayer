# Daemon JVM criteria for the demo build, which has its own wrapper and is built by CI without the
# root build running. The same as the root build's gradle/gradle-daemon-jvm.properties, which
# carries the reasoning: Java 17 of any vendor, found locally, and a named failure without one.
toolchainVersion=17
