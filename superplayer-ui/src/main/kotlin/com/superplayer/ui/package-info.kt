/*
 * superplayer-ui — Optional Compose player surface (thin; app can bring its own)
 *
 * Placeholder. This module is created empty and buildable so that the module boundary and
 * dependency direction are fixed and enforceable now, rather than negotiated once code
 * arrives. PRD.md Part 4 does not yet schedule this module — see docs/modules.md. It
 * intentionally declares no public API yet.
 */
package com.superplayer.ui
